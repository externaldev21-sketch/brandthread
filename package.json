{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "db:purge-test-data": "pnpm --filter @workspace/api-server run db:purge-test-data",
    "qa:seed": "pnpm --filter @workspace/api-server run qa:seed",
    "qa:crawl": "pnpm --filter @workspace/mobile run qa:crawl"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "@replit/revenuecat-sdk": "^5.0.0"
  },
  "devDependencies": {
    "playwright": "1.56.1",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
