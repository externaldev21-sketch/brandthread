/**
 * Legacy "Account control" route. Account deletion now lives in a single,
 * server-backed flow for buyer and seller accounts; older links land there.
 */
export { default } from './delete-account';
